# LIL WHOAMI

This is an APP REPOSITORY (owned by developers), without environment logic.

```bash
.
  src           # the application Source Code
  Dockerfile    # the application Container 
  helm/         # the application Helm Chart
    Chart.yaml
    values.yaml
    templates/
```